apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: trisla-dashboard-monitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: trisla-dashboard
  endpoints:
    - port: backend
      interval: 15s
      path: /metrics


