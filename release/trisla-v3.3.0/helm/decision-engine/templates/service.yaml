apiVersion: v1
kind: Service
metadata:
  name: decision-engine
  labels:
    app.kubernetes.io/name: decision-engine
    app.kubernetes.io/instance: decision-engine
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: decision-engine
    app.kubernetes.io/instance: decision-engine
