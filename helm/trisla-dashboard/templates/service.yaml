apiVersion: v1
kind: Service
metadata:
  name: trisla-dashboard
spec:
  selector:
    app: trisla-dashboard
  ports:
    - name: backend
      port: 5000
      targetPort: 5000
    - name: frontend
      port: 5174
      targetPort: 5174


