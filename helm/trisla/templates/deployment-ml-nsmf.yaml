apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "trisla.name" . }}-ml-nsmf
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "trisla.labels" (merge . (dict "component" "ml-nsmf")) | nindent 4 }}
spec:
  replicas: {{ .Values.mlNsmf.replicas }}
  selector:
    matchLabels:
      {{- include "trisla.selectorLabels" (merge . (dict "component" "ml-nsmf")) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "trisla.selectorLabels" (merge . (dict "component" "ml-nsmf")) | nindent 8 }}
    spec:
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      containers:
      - name: ml-nsmf
        image: {{ include "trisla.image" (merge .Values.mlNsmf.image (dict "Values" .Values)) }}
        ports:
        - containerPort: {{ .Values.mlNsmf.service.port }}
          name: http
        env:
        - name: TRISLA_NODE_INTERFACE
          value: {{ .Values.network.interface | quote }}
        - name: TRISLA_NODE_IP
          value: {{ .Values.network.nodeIP | quote }}
        - name: OTLP_ENDPOINT
          value: "http://{{ include "trisla.name" . }}-otel-collector:4317"
        - name: KAFKA_ENABLED
          value: {{ .Values.mlNsmf.env.KAFKA_ENABLED | default "false" | quote }}
        - name: KAFKA_BROKERS
          value: {{ .Values.mlNsmf.env.KAFKA_BROKERS | default "" | quote }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          {{- toYaml .Values.mlNsmf.resources | nindent 10 }}




