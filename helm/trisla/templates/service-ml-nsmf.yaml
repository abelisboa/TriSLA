apiVersion: v1
kind: Service
metadata:
  name: {{ include "trisla.name" . }}-ml-nsmf
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "trisla.labels" (dict "Values" .Values "Release" .Release "Chart" .Chart "component" "ml-nsmf") | nindent 4 }}
spec:
  type: {{ .Values.mlNsmf.service.type }}
  ports:
  - port: {{ .Values.mlNsmf.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: trisla-ml-nsmf
    app.kubernetes.io/name: {{ include "trisla.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}































