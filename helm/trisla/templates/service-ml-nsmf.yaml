apiVersion: v1
kind: Service
metadata:
  name: {{ include "trisla.name" . }}-ml-nsmf
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "trisla.labels" (merge . (dict "component" "ml-nsmf")) | nindent 4 }}
spec:
  type: {{ .Values.mlNsmf.service.type }}
  ports:
  - port: {{ .Values.mlNsmf.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "trisla.selectorLabels" (merge . (dict "component" "ml-nsmf")) | nindent 4 }}




