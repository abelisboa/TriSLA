apiVersion: v1
kind: Service
metadata:
  name: {{ include "sla-agents.fullname" . }}
  labels:
    {{- include "sla-agents.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "sla-agents.selectorLabels" . | nindent 4 }}




