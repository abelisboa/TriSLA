# ============================================
# Alertmanager Configuration - TriSLA
# EXEMPLO - Copiar para config.yml e preencher com credenciais reais
# ============================================

global:
  resolve_timeout: 5m
  
  # Configuração de email - PREENCHER COM CREDENCIAIS REAIS
  smtp_smarthost: 'smtp.gmail.com:587'  # Ajustar conforme seu provedor
  smtp_from: 'trisla-alerts@example.com'  # Seu email de envio
  smtp_auth_username: 'seu-email@gmail.com'  # Seu email
  smtp_auth_password: 'sua-senha-app'  # Senha de app (não senha normal)
  smtp_require_tls: true

# ... resto da configuração igual ao config.yml original ...

