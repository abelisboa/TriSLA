apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bc-nssmf-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: trisla-bc-nssmf
  namespaceSelector:
    matchNames:
      - trisla
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
