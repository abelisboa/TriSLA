MISSÃO

Refatorar completamente a geração de gráficos da campanha V6.2 para nível IEEE, sem alterar dataset.

ENTRADA

evidencias_resultados_trisla_baseline_v13_2/dataset/

OBJETIVO

Transformar gráficos atuais em evidência científica clara de SLA-aware behavior por slice.

REQUISITOS

1. Todos gráficos devem:
   - ter título acadêmico completo
   - eixos nomeados com unidades
   - legenda obrigatória
   - sem abreviações ambíguas (clean, raw, etc)

2. Implementar curvas:
   - binning (ex: 10 bins)
   - média por bin
   - plotar linha sobre scatter

3. Gerar novos gráficos:

A) URLLC
- score vs RTT (scatter + curva)
- score vs jitter (scatter + curva)

B) eMBB
- score vs PRB (curva principal)
- decisão vs PRB (stacked ou cor)

C) mMTC
- score vs CPU (curva)
- score vs memória (curva)

D) Cross-slice
- heatmap interpretável (slice × domínio)
- distribuição de decisões por cenário

4. Padronizar nomes de arquivos:

v62_urllc_latency_sensitivity.png
v62_embb_ran_resource_sensitivity.png
v62_mmtc_core_resource_sensitivity.png
v62_slice_domain_influence_heatmap.png

5. Salvar também:
- CSV das curvas (bins + média)

SAÍDA

figures_ieee_v62/
analysis/curves_v62/