Cenário eMBB — 20 SLAs simultâneos

Rodada: R1 (baseline válida)
Escopo: Avaliação de escalabilidade lógica no eixo eMBB
Ambiente: NASP real
Isolamento: TOTAL (somente experiments/)

🎯 Objetivo Científico

Avaliar o comportamento real do TriSLA sob carga eMBB elevada (20 SLAs simultâneos), verificando:

Estabilidade do pipeline end-to-end

Consistência das decisões (ACCEPT / RENEG / REJECT)

Rastreabilidade completa

Limites operacionais sem alterar regras ou código

📌 Pré-requisitos (OBRIGATÓRIOS)

Antes de qualquer comando, verifique e documente:

ssh porvir5g@node006
cd /home/porvir5g/gtp5g/trisla

Checklist pré-condicional
kubectl -n trisla get pods
kubectl -n trisla get svc
kubectl -n monitoring get pods


Critério:

Pods em CrashLoopBackOff antigos são permitidos, desde que:

SEM-CSMF esteja Running

Decision Engine esteja Running

ML-NSMF esteja Running

NASP Adapter esteja Running

➡️ Documentar em:

experiments/reports/C1.3_R1_PRE_CONDITIONAL_STATUS.md

📁 FASE 1 — Congelamento Experimental
cd experiments
mkdir -p reports/C1.3_R1


Congelar artefatos:

sha256sum \
  templates/C1_eMBB_20.yaml \
  generators/submit_slas.py \
  collectors/prometheus_collector.py \
  collectors/logs_collector.py \
  analysis/compute_metrics.py \
  > reports/C1.3_R1/SHA256_INPUTS.txt

🚀 FASE 2 — Execução do Cenário
Submissão controlada (SEQUENCIAL)
python3 generators/submit_slas.py \
  --template templates/C1_eMBB_20.yaml \
  --output reports/C1.3_R1/C1.3_submissions.json \
  --delay 5


Regras obrigatórias:

❌ Sem paralelismo

❌ Sem retry automático

✅ correlation_id único por SLA

✅ 20 SLAs submetidos

Validar saída:

jq length reports/C1.3_R1/C1.3_submissions.json
# Deve retornar: 20

📊 FASE 3 — Coleta de Dados
Métricas Prometheus (brutas)
python3 collectors/prometheus_collector.py \
  --scenario C1.3_R1 \
  --output reports/C1.3_R1/

Logs Kubernetes
python3 collectors/logs_collector.py \
  --scenario C1.3_R1 \
  --output reports/C1.3_R1/

📐 FASE 4 — Análise Offline
python3 analysis/compute_metrics.py \
  --input reports/C1.3_R1/C1.3_submissions.json \
  --output reports/C1.3_R1/analysis_C1.3_R1.json


Resultados esperados no JSON:

total_slas

decision_counts

tempos (min / média / p95)

🧾 FASE 5 — Preservação de Evidências
cd reports
tar -czf C1.3_R1.tgz C1.3_R1
sha256sum C1.3_R1.tgz > C1.3_R1_SHA256.txt

✅ FASE 6 — Checklist Final

Criar:

experiments/reports/C1.3_R1/CHECKLIST_VALIDACAO.md


Marcar explicitamente:

 Execução sequencial

 20 SLAs submetidos

 Pipeline end-to-end funcional

 Decisões registradas

 Logs coletados

 Evidências preservadas

 Nenhum código TriSLA alterado

🎓 Critério de Conclusão (SEM DISCUSSÃO)

A execução SÓ é válida se:

C1.3_R1.tgz existir

SHA256 registrado

Checklist preenchido

Nenhuma modificação fora de experiments/

Resultados coerentes com C1.1_R1 e C1.2_R1
