1. Objetivo

Este prompt define o protocolo experimental para estabilização do NASP Adapter e implantação manual de NSI/NSSI, respeitando:

Execução manual e dirigida

Sem encadeamento automático

Sem alterar código

Apenas ações operacionais permitidas

2. Ambiente de Execução

Host SSH: node006

Namespace: trisla

Diretório base:

/home/porvir5g/gtp5g/trisla

3. FASE 0 — Pré-check do NASP Adapter
kubectl get pods -n trisla | grep nasp
kubectl get svc -n trisla | grep nasp


Registrar estado inicial em:

docs/experiments/C5_NASP/FASE0_ESTADO_INICIAL.md

4. FASE 1 — Diagnóstico do CrashLoopBackOff (NASP Adapter)
kubectl logs trisla-nasp-adapter-5bfc4bd846-b78sh -n trisla --previous


Objetivo:

Confirmar erro transitório

Confirmar dependência indisponível no startup

Não corrigir código

5. FASE 2 — Estabilização operacional
5.1 Restart controlado
kubectl rollout restart deployment trisla-nasp-adapter -n trisla
kubectl rollout status deployment trisla-nasp-adapter -n trisla

5.2 Alternativa
kubectl delete pod -n trisla -l app=trisla-nasp-adapter

6. FASE 3 — Validação do NASP Adapter
kubectl get pods -n trisla | grep nasp
kubectl logs -n trisla -l app=trisla-nasp-adapter --tail=50


Critério:

Pelo menos 1 pod estável

Endpoint HTTP ativo

7. FASE 4 — Implantação manual de NSI/NSSI (laboratório)
7.1 Teste de saúde
curl http://<IP_DO_NASP_ADAPTER>:8085/health

7.2 Implantação manual (payload mínimo)

Executar uma única implantação de laboratório:

curl -X POST http://<IP_DO_NASP_ADAPTER>:8085/nsi/deploy \
  -H "Content-Type: application/json" \
  -d @payload_nsi_laboratorio.json


📌 Payload mínimo, previamente documentado, sem automação.

8. Critério de Saída do PROMPT_C5_NASP_MANUAL

✅ NASP Adapter estabilizado

✅ Implantação manual executada

✅ Logs coletados

✅ Evidência registrada

❌ Sem automação

❌ Sem alteração de código

9. Organização dos Artefatos
docs/experiments/
├── C5_E3/
│   ├── FASE0_ESTADO_INICIAL.md
│   ├── FASE3_ESTADO_POS_ESTABILIZACAO.md
│   └── LOGS/
└── C5_NASP/
    ├── FASE0_ESTADO_INICIAL.md
    ├── FASE4_IMPLANTACAO.md
    └── LOGS/
