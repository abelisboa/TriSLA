apiVersion: v1
kind: Service
metadata:
  name: {{ include "trisla.name" . }}-ui-dashboard
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "trisla.labels" (merge . (dict "component" "ui-dashboard")) | nindent 4 }}
spec:
  type: {{ .Values.uiDashboard.service.type }}
  ports:
  - port: {{ .Values.uiDashboard.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "trisla.selectorLabels" (merge . (dict "component" "ui-dashboard")) | nindent 4 }}




