import React from 'react'
import { Link, useLocation } from 'react-router-dom'
function NavItem({to, children}:{to:string; children:React.ReactNode}){const { pathname } = useLocation();const active = pathname === to;return(<Link to={to} className={`block rounded-lg px-3 py-2 ${active?'bg-white/15 text-white':'text-white'} hover:text-brand-200 hover:bg-white/10`}>{children}</Link>)}
export default function MainLayout({ children }:{children:React.ReactNode}){return(<div className="min-h-screen flex bg-gray-50"><aside className="w-64 bg-sidebar text-white p-6 hidden md:block"><div className="text-2xl font-bold mb-6">TriSLA</div><nav className="space-y-1"><NavItem to="/">📊 Dashboard</NavItem><div className="text-xs uppercase text-white/70 mt-4 mb-1 px-3">Slices</div><NavItem to="/slices">🧩 Lista</NavItem><NavItem to="/slices-npl">💬 Criar Slice (NPL)</NavItem><NavItem to="/slices-gst">🧮 Criar Slice (GST)</NavItem><div className="text-xs uppercase text-white/70 mt-4 mb-1 px-3">Operação</div><NavItem to="/metrics">📈 Métricas</NavItem><NavItem to="/templates">🧠 Templates</NavItem><NavItem to="/admin">🧑‍💻 Admin</NavItem></nav></aside><main className="flex-1 p-4 md:p-8">{children}</main></div>)}